version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.16

    working_directory: ~/app
    steps:
      - checkout

      # specify any bash command here prefixed with `run: `
      - run: go get -v -t -d ./...
      - run: go test -v ./...
